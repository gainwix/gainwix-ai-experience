---
name: gx-next
description: (Coming soon) Advance the deployment workflow to the next step.
disable-model-invocation: true
---

**`/gx-next` is not implemented yet in this build.**

Intended purpose: a workflow driver — advance to the next logical step in your deployment journey (e.g. from onboarding to dry run to deploy), with the workmate suggesting what's sensible next.

For now, the path is: `/gx-init` → `/gx-sim` → `/gx-gcp-deploy`. Tell the developer `/gx-next` is coming soon and point them at that path. Take no other action.
