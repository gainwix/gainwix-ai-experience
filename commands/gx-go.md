---
name: gx-go
description: (Coming soon) Drive the deployment workflow forward end to end.
disable-model-invocation: true
---

**`/gx-go` is not implemented yet in this build.**

Intended purpose: a workflow driver that carries the deployment forward end to end with minimal prompting — the "just take me through it" command, respecting the same trust gates as `/gx-gcp-deploy`.

For now, use `/gx-gcp-deploy` for the full flow. Tell the developer `/gx-go` is coming soon and take no other action.
