---
name: gx-reset
description: (Coming soon) Reset GainWix plugin configuration back to a clean state.
disable-model-invocation: true
---

**`/gx-reset` is not implemented yet in this build.**

Intended purpose: reset the GainWix plugin configuration (connectors, MCPs, cached state) back to a clean starting point — useful when onboarding a new project or recovering from a bad config. Will not touch your cloud resources, only local plugin config.

For now, tell the developer it's coming soon. Do not modify any configuration or delete anything.
