---
name: gx-add
description: (Coming soon) Add a connector or MCP to your GainWix setup. Configuration, not coding.
argument-hint: <connector|mcp>
disable-model-invocation: true
---

**`/gx-add` is not implemented yet in this build.**

Intended purpose: add a connector or MCP server to your GainWix configuration — for example a database connector, a secrets provider, or an additional cloud capability — so the workmate can use it during deploys. This is a configuration action, not a coding task.

For now, tell the developer it's coming soon and point them at `/gx-init` to set up the repo (scaffolding) and `/gx-gcp-deploy` to deploy (it handles GCP). Do not attempt to modify any configuration.
