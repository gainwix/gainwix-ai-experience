---
name: gx-issue
description: (Coming soon) Track deployment work — add, list, or remove issues.
argument-hint: add|list|rm [args]
disable-model-invocation: true
---

**`/gx-issue` is not implemented yet in this build.**

Intended purpose: lightweight work/issue tracking for deployment tasks — `add` to record a follow-up, `list` to see open items, `rm` to close one. Lets the workmate keep a running list of deployment work alongside your code.

Requested subcommand: **$ARGUMENTS**

For now, tell the developer it's coming soon. Do not create or modify any tracking state.
